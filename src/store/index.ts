import { createStore } from "vuex";
import gameStore from "@/store/gameStore";

export default createStore({
  // state: {},
  // getters: {},
  // mutations: {},
  // actions: {},
  modules: {
    gameStore,
  },
});
